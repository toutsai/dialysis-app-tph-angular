# 修正紀錄 — 2026-05-27

本日針對「我的病人列表備物計算」、「訊息中心左欄」、「前端快取」與「stats 備物 popover 定位」進行修正。

## 改動檔案清單（7 個）

```
前端 angular-client/src/
  utils/scheduleUtils.ts
  app/features/my-patients/my-patients.component.ts
  app/features/my-patients/my-patients.component.html
  app/features/collaboration/collaboration.component.ts
  app/components/preparation-popover/preparation-popover.component.ts
  app/components/preparation-popover/preparation-popover.component.css
後端
  src/index.js
```

---

## ① 我的病人列表：備物 AK 改為「當次輪替」計算

**檔案**
- `angular-client/src/utils/scheduleUtils.ts`（新增 `getWeeklySessionIndex()`、`resolveDailyRotationValue()`）
- `angular-client/src/app/features/my-patients/my-patients.component.ts`
- `angular-client/src/app/features/my-patients/my-patients.component.html`

**問題**
透析醫囑 AK 以 `/` 分隔代表「一週各次透析輪替使用不同人工腎臟」（例 `21S/Hi23/Hi23`），但備物計算（`getShiftSupplySummary`）原本把三段全數累計，導致一位病人被算成 3 顆 AK。

**修正**
- 病人卡片 **AK 仍顯示完整字串**（`preparation.ak`），滑鼠移上去 tooltip 提示「今日備物」。
- 備物**數量計算改用當天該次的 AK**（新增 `preparation.akToday`），依病人頻率 + 星期推算「本週第幾次透析」取對應段。
- 例：洗 一三五、週三 = 本週第 2 次（index 1）→ 當天 AK = `Hi23`，備物只計 `Hi23×1`。
- 無法判斷次序（頻率未知 / 當天非透析日 / 段數不足）時，保守回傳完整原值，避免漏備。

**重要：freq 來源**
頻率已統一在 PatientView 編輯，存於病人 `schedule_rule` / 總表 `base_schedules` 的 `MASTER_SCHEDULE`，前端對應 **`patient.scheduleRule.freq`**。
**勿用頂層 `patient.freq`** —— 後端那是 `dialysis_orders.freq`，現已淘汰為空字串，會害解析退回「列全部」。

**狀態**：✅ 完成、已驗證（曹文榮 `21S/Hi23/Hi23` + 一三五 + 週三 → `Hi23`）

---

## ② 訊息中心（CollaborationView）左欄病人空白

**檔案**：`angular-client/src/app/features/collaboration/collaboration.component.ts`

**問題**：護理師登入後左欄「我照顧的病人」一律是空的。

**修正（三處）**
1. **解包 nurse_assignments**：`names` 與真正的 `teams` map 都巢狀包在 `teams` JSON 欄位內（schema 只有 `teams` 欄位）。原本讀 `assignmentData.names` 恆為 `undefined` → `if (names && teams)` 永遠不成立 → 左欄恆空。已加解包（相容巢狀與扁平）。
2. **UUID-safe 的 patientId**：teams 的 key 是 `${patientId}-${shift}`，但 patientId 可能是含「-」的 UUID（如 `c40f52cb-5476-...-early`）。改用 `key.substring(0, key.lastIndexOf('-'))`，避免 `split('-')[0]` 截斷。
3. **改用 `fetchById(date)`**：載當日排程與護理分配，由 `schedulesApi.fetchAll().filter(date)` 改為 `fetchById(date)`，對齊可運作的 my-patients 與舊 Vue 版（`fetchByDate`）。

**參考**：舊 Vue 版 `D:\dialysis-app\洗腎平台原始碼\src\views\CollaborationView.vue` 使用 `schedulesApi.fetchByDate(date)` + `fetchNurseAssignments(date)`，且 `assignments` 直接帶 `.names`/`.teams`。

**狀態**：✅ 已部署（DB 模擬驗證：5/27 謝常菁「早I」→ 當日班表 8 人）

---

## ③ 後端：靜態檔快取標頭（根治「重整看不到更新」）

**檔案**：`src/index.js`

**問題**：前端改動部署後，使用者重整仍載到舊程式 —— 因瀏覽器快取了檔名固定的 `index.html`，它又指向舊的 hash bundle。

**修正**
- `index.html` → `Cache-Control: no-cache, no-store, must-revalidate`（每次重整都重新抓最新）。
- 帶內容雜湊的 JS/CSS（如 `main-XXXX.js`）→ `Cache-Control: public, max-age=31536000, immutable`（安全長期快取）。

**效果**：自此一般 F5 重整即可看到更新，不必每次 Ctrl+Shift+R。
（加標頭「之前」就開著頁面的使用者，需再硬重整一次過渡，之後永久生效。）

**狀態**：✅ 完成、已驗證標頭生效

---

## ④ stats 備物 popover 定位（尚未根治）

**檔案**
- `angular-client/src/app/components/preparation-popover/preparation-popover.component.ts`
- `angular-client/src/app/components/preparation-popover/preparation-popover.component.css`

**問題**：點同一顆 📋 備物 icon 多次，popover 出現在不同位置。期望固定在 icon 右下角。

**已做**：`updatePosition()` 改成只用 icon 的 `getBoundingClientRect()` 定位（左上角錨定 icon 右下角）、動態 `max-height` + 內部捲動、表頭 sticky 吸頂；切換 icon 才重算位置。

**狀態**：⚠️ **仍未根治**（使用者回報同一 icon 仍跑不同位置）。待查方向：
1. 容器捲動使 `position:fixed` 的 icon viewport 座標每次不同 → 先在瀏覽器 `console.log(rect, top, left)` 確認哪個值在變。
2. 水平捲動表格內 rect 變動。
3. 終極解法考慮改用 CDK Overlay 或 render 到 `document.body` 並用頁面絕對座標。

---

## 其他（非程式碼）

- 將 `D:\dialysis-app`（production，port 3000 的 `dialysis-server`）的資料庫複製覆蓋到本資料夾。
  - 複製前先備份舊 DB 至 `data\_pre-copy_20260527_005743\`。
  - 連同未 checkpoint 的 `-wal`/`-shm` 一起複製，重啟後 WAL 已 checkpoint 併入主檔。
- 過程中的暫存查詢腳本（`tmp_*.mjs`）皆已刪除。

## 部署備註

- 前端：`npm run build:angular` → `pm2 restart dialysis-server-angular`（port 3001，**勿碰 `dialysis-server`**）。
- 後端（`src/index.js` 等）：同一個 `pm2 restart dialysis-server-angular` 一併載入。
- 本日改動皆已 build + restart 上線。
